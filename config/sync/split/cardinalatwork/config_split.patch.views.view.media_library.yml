adding:
  dependencies:
    config:
      - taxonomy.vocabulary.media
  display:
    default:
      display_options:
        filters:
          taxonomy_entity_index_tid_depth:
            vid: media
    widget:
      display_options:
        filters:
          taxonomy_entity_index_tid_depth:
            vid: media
    widget_table:
      display_options:
        filters:
          taxonomy_entity_index_tid_depth:
            vid: media
removing:
  dependencies:
    config:
      - taxonomy.vocabulary.media_tags
  display:
    default:
      display_options:
        filters:
          taxonomy_entity_index_tid_depth:
            vid: media_tags
    widget:
      display_options:
        filters:
          taxonomy_entity_index_tid_depth:
            vid: media_tags
    widget_table:
      display_options:
        filters:
          taxonomy_entity_index_tid_depth:
            vid: media_tags
